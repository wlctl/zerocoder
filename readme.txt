ты агент разработчик, мы разрабатываем продукт LogAnalzer (la) для инженеров тех.поддержки и аналитиков по анализу и визуализации лог-файлов заказчиков.

1.твоя задача получать юзерстори (ЮС) и фиксировать их в @user-stories в текстовом формате и в служебном формате максимально удобным тебе для обработки (предположу json и md)

2.каждая текстовая ЮС может быть поправлена как через тебя так и внешним образом через текстовый редактор (в дальнейшем через гит), те изменения ЮС служебного формата необходимо проверять и далее отражать в архитектурных артефактах/схемах, коде, тестах, документации, changelog и релизе продукта.

3.базовая генерация релиза (каталог release) на go, но при этом при запросе также в полном объеме функционала должны иметь возможность получить релиз на python.

4.в каталоге sh должны быть скрипты start-la.sh, stop-la.sh, status-la.sh для старта, останова и проверки статуса процесса.

5.продукт состоит из двух независимо подключаемых и изменяемых проектов 1.backend (py/go) 2.frontend (angular)

отрази это дополнительно в CLAUDE.md и settings.local.json 
далее начнем формировать ЮС и формировать функциональные и нефункциональные требования к продукту.

первый поток бекенд, для простоты (тк делаем MVP) Postgres не используем, используй sqlite.
приложение использует la.conf который читается при старте процесса в котором будем указывать юрл подключения к БД (SOURCE_DB_URL), по дефолту sqlite и эти структуры ты создаешь автоматически при остуствии БД sqlite la.db
la.conf создается при отстуствии из темплейта la.conf.template SOURCE_DB_URL, LISTEN_ADDRESS (localhost), LISTEN_PORT (8888)
в sqlite таблицы и прочие объекты ты создаешь при их остутствии
поддержку Postgres добавим позже пока на этом не концентрируемся

пиши юзерстори
1.пользователь загружает один или несколько лог файлов или зип архив через веб интерфейс
максимальный размер файла 10Г (MAX_FILE_SIZE)
максимальное число файлов 10 (MAX_FILE_COUNT)

2.лог файлы могут быть разнородными - alert.log базы данных Оракл, лог файл БД Postgres, многострочные лог файлы java weblogic/tomcat/glassfish, линуксовые лог файлы.

3.при загрузке загружаемый файл и его мд5 сохранаются в таблице, повторная загрузка блокируется сообщение "Файл уже был загружен ранее"

4.пользователь может открыть ранее загруженные логи и повторно перейти к их анализу, либо удалить эти файлы.

5.загруженные файлы преобразуются в таблицы 

6.работаем только с текстовыми лог файлами.каждый текстовый лог файл должен разбираться и сохраняться строкой лог файла в строке таблицы, блоб не используй, используй длинное текстовое поле. особенно важно уметь выделять датувремя события (с учетом разных форматов дат и таймзон), критичность (error/warning/etc)

7.зип архив только с лог файлами но может содержать подкаталоги

8.мультилайн джава логи (и остальные лог файлы тоже) могут содержать многострочные стектрейсы, те датавремя, критичность и начало сообщения в одной строке и далее идет продолжение стектрейса до следующего сообщения с датойвременем критичностью и тп эти многострочные сообщения нужно парсить с сохранением как одну строку таблицы

1.зип считаем как 1. при загрузке контролируем мд5 входящего файла. далее после распаковки архива каждому файлу новый ID. вероятно таблицы t_files_upload (вход) 1->N t_files_analyze (распакованные файлы)
2.автомат, если не определилось показывать как текстовый файл. в принципе если пользователь загрузил текстовый конф.файл его также нужно иметь возможность просмотреть.
3.да давай введем LA_DEFAULT_TZ как дефолтную в виде UTC но изменяемую

фиксируй НФР (нефункциональные требования)
nodejs используется только на этапе сборки в поставке идет собранный набор джаваскриптов, нодежс не поставляется и не устанавливается на ПК пользователя. дистрибутив на ПК пользователя только как зип архив, после распаковки модифицирует при необходимости дефолтный la.conf, выполняет sh-файл, процесс стартует, при первом запуске создает дефолтную склайт бд, стартует веб-сервер со смонтированным агудар приложением.

фиксируй НФР (нефункциональные требования)
приложение должно уметь определять тип лога и использовать разные парсеры
каждый парсер должен быть в виде отдельного бинарного so-файла для го и py-файла для питоновского релиза
парсеры должны быть в подкаталоге parsers, добавление нового парсера должно подхватываться "на лету"
модификация исходного когда парсера не должна затрагивать весь проект и требовать его полной пересборки
примеры логов 

фиксируй ФР

форматы дат могут быть разными это нужно уметь определять в разных форматах и локалях

добавь этап постобработки.
после загрузки логи или зип и последующего парсинга файла каждый файл
должен иметь сводку по файлу те кроме парсера к каждому типу файла должен быть свой постобработчик
базовый постобработчик может показывать число строк дату загрузки размер файла кодировку итп
остальные постобработчик наследуют от базового и могут расширять информацию по событиям как минимум нужно уметь выделять число сессий старт-стоп, длительность интервалов старт-стоп, число сообщений в категорях (error/warning/critical/etc)

===
фиксируй ФР для фронтенда

1.после загрузки файл показывается в таблице загрузок (сортируемая, фильтруемая) с возможностью удаления файла и всех результатов его обработки, поля - размер, дата загрузки, сводка из парсинга если файл один лог, если архив - число файлов архиве. под таблицей общая информацию по размеру хранилища, числу загрузок и тп
2.выбрав файл пользователь проваливается в просмотр. где в шапке детали архива или детали файла
3.далее селектор файлов со списком файлов с чекбоксами, по умолчанию выбраны все файлы.
4.далее поле поиска. фильтрует по вхождению текста в файле по всем полям (возможно одно дополнительное исходное поле). поиск во всех файлах файлового селектора. может быть более одного фильтра. каждый фильтр может удаляться независимо от других.
5.далее под детализацией идет таймлайн селектор (если применимо) - минимальная-максимальные дата, с возможностью двигать границы селектора. 
6.далее подкраска строк файла. поле ввода текста (с опциональным выбором кластерных ликсем лога) и виджет указани цвета - все строки подкрашиваются.
7.все результаты фильтрации подкраски сохраняются между рестартами процесса (в таблицах). очистка форматирования и фильтров по отдельной кнопке.
8.каждый файл показывается в отдельной таблице строк по 10 (или динамически изменяемо), таблицы идут одна за другой, над таблицей идет название файла и краткая сводка мелким читаемым шрифтом. таблица может быть закрыта и тогда чекбокс снимается в селекторе. если чекбокс отмечен таблица показывается вновь.
9.если возможно каждый файл должен иметь возможность открытия в новом окне но с управлением от основной страницы. если таблица открыта в отдельном окне на основной странице она не показывается более.
10.дополнительно по каждой таблице может быть показан график событий с переключаемой группировкой числа событий месяц/день/час/минута - нужно видеть всплески событий.



1. Парсинг не true-streaming — MAX_FILE_SIZE должен защищать проверяя загружаемый файл. размер файла при распаковке при этом не проверяем. это ок. уточним позже.
2. хорошо бы иметь в ЮИ возможность загружать несколько файлов одновременно. интерфейс должен быть юзерфрендли максимально.
3. да отложен, пока сосредоточимся на том что есть.
4. конфиг берёт дефолты из la.conf, если заданны переменные окружения то должны перекрывать la.conf. но не критично.
5. не понял, посмотрю ЮИ уточню позже.
6. ок
7. собирай so-плагины и зип дистрибутив. подготовь мд документацию и помести в дистрибутив





